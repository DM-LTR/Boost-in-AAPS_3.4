# Copy to keystore.properties (which is gitignored) and fill in YOUR values.
# Build will sign 'release' only when keystore.properties exists; otherwise it falls back to unsigned/debug.
storeFile=keystore/your-keystore.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
